# 主角系统：配置扩表规范（V1.0）

本文定义**可独立扩充的配表格式**。新增任务、奖励、事件、卡池行、皮肤、签到档位等，**只增数据文件、不改业务代码**即可接入，前提是：字段齐全、引用有效、通过 §10 校验。

**运行时必读**：《主角系统-游戏机制总纲》中的派发顺序与状态机。  
**语义与玩法细节**：《主角系统-属性任务与抽奖细化》。

---

## 1. 全局约定

### 1.1 文件与版本

| 规则 | 说明 |
|------|------|
| 格式 | 推荐 **YAML** 或 **JSON**（二选一全项目统一）；下文示例用 YAML |
| 编码 | UTF-8 |
| 每条配置 | 必须有 **`id`**（全游戏唯一、稳定不变，用于存档引用） |
| 版本 | 每个文件顶部 **`schemaVersion: 1`**；不兼容升级时递增 |
| 合并策略 | 多文件**按目录合并**为一张逻辑表（如 `rewards/*.yaml` → `RewardBundleDef[]`） |

### 1.2 ID 命名建议（前缀）

| 前缀 | 用途 |
|------|------|
| `rb_` | RewardBundle（奖励包） |
| `task_` | Task（任务定义） |
| `evt_` | Event（随机事件） |
| `pool_` | GachaPool（卡池） |
| `gacha_` | 池内一行掉落 |
| `skin_` | 皮肤 |
| `item_` | 可进背包的道具（果实、碎片、气泡等） |
| `skill_` | 技能卡定义 |
| `buff_` | Buff 定义 |
| `sign_` | 签到档位规则 |

### 1.3 枚举（代码与配表必须一致）

**宿主属性键 `statKey`（加减量用）**

| 值 | 含义 |
|----|------|
| `FORTUNE` | 气运 |
| `VITALITY` | 能量 |
| `MOOD` | 心境 |
| `BOND` | 羁绊 |
| `FOCUS` | 专注 |

**货币键 `currencyId`**

| 值 | 含义 |
|----|------|
| `STAR_DUST` | 星尘 |
| `MOON_GLOW` | 月华 |
| `GACHA_TICKET_NORMAL` | 普通祈愿券（示例） |

**派发原因 `reasonCode`（只增不改语义）**

`SIGN_IN` · `TASK_COMPLETE` · `TASK_VERIFY` · `EVENT_BRANCH` · `GACHA` · `SHOP` · `ITEM_USE` · `ADMIN` · `MIGRATION`

**任务类型 `taskType`**

`A` · `B` · `C` · `D` · `F` · `G` · `H`（`E` 为签到体系专用时可不占任务表）

---

## 2. 奖励包 `RewardBundleDef`（一切产出的原子）

**路径建议**：`config/rewards/*.yaml`  
**一条 = 一个 `id`**。任务、事件、签到等**只引用 `rewardBundleId`**，不内联数值。

### 2.1 字段规范

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `id` | 是 | string | 如 `rb_task_a_small` |
| `schemaVersion` | 是 | int | 与文件级一致即可 |
| `currencies` | 否 | map | `currencyId → int`（≥0） |
| `statDelta` | 否 | map | `statKey → int`（可负；最终由引擎夹逼到宿主上下限） |
| `items` | 否 | list | 见下表 |
| `buffs` | 否 | list | 见下表 |
| `telemetryTags` | 否 | list[string] | 分析用标签，不参与逻辑 |

**`items[]` 元素**

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `itemId` | 是 | string | 须存在于 `ItemDef` |
| `count` | 是 | int | ≥1 |
| `grantType` | 否 | enum | `STACK`（默认堆叠）\|`UNIQUE` |

**`buffs[]` 元素**

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `buffId` | 是 | string | 须存在于 `BuffDef` |
| `durationSec` | 是 | int | ≥0；0 表示由 BuffDef 默认 |

### 2.2 示例（最小 + 完整）

```yaml
# config/rewards/rb_examples.yaml
schemaVersion: 1
bundles:
  - id: rb_empty
    # 纯叙事用：无任何数值

  - id: rb_task_a_small
    statDelta:
      MOOD: 2
      BOND: 1
    telemetryTags: [task, tier_a]

  - id: rb_task_verify_bonus
    currencies:
      STAR_DUST: 15
    statDelta:
      VITALITY: 5
    telemetryTags: [task, verified]

  - id: rb_sign_day3
    currencies:
      STAR_DUST: 30
      GACHA_TICKET_NORMAL: 1
    statDelta:
      BOND: 3
```

### 2.3 自洽规则

- 引擎对 `statDelta` 应用后执行 **clamp**（默认每 stat `0～100`，羁绊若走经验制则在映射层处理）。
- **禁止**在任务/事件里重复写同一奖励数值；一律 `rewardBundleId` 指向本表。

---

## 3. 任务 `TaskDef`

**路径建议**：`config/tasks/*.yaml`

### 3.1 字段规范

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `id` | 是 | string | |
| `taskType` | 是 | enum | §1.3 |
| `titleKey` / `title` | 是 | string | 或 i18n key |
| `descKey` / `desc` | 否 | string | |
| `rewardBundleId` | 是 | string | **自述/基础完成**时派发；须存在 `RewardBundleDef` |
| `rewardVerifiedBundleId` | 否 | string | 存在验证档时**验证通过**额外派发；无则省略 |
| `group` | 否 | enum | `DAILY`（默认）\|`WEEKLY` \|`STORY` |
| `maxCompletionsPerDay` | 否 | int | 默认 1；A 类可 >1 配合递减 |
| `diminishingReturns` | 否 | bool | 为 true 时走引擎 A 类递减逻辑 |
| `handlerParams` | 否 | object | **类型专有参数**，见 §3.2 |
| `unlockCondition` | 否 | object | 见 §3.3 |
| `countsForWeekly` | 否 | bool | 是否计入周常「验证类次数」等（默认按 taskType 引擎推断，可覆盖） |

### 3.2 `handlerParams`（按 `taskType` 扩展）

仅填本类型需要的键；**未知键忽略**，避免旧客户端崩。

| taskType | 建议字段 | 说明 |
|----------|----------|------|
| B | `durationSec` | 计时秒数 |
| B | `pauseOnBackground` | bool |
| C | `healthMetric` | `STEPS` \|`SLEEP_HOURS` 等 |
| C | `threshold` | int |
| D | `minUserTurns` | int |
| F | `sceneTagsExpected` | list[string] 识图用 |
| G | `minDurationSec` | int 人声检测 |

### 3.3 `unlockCondition`（可选）

```yaml
unlockCondition:
  minBond: 10          # 羁绊 ≥
  requiredFlags: []    # 剧情 flag，可选
  requiredItems: []    # 可选
```

### 3.4 示例

```yaml
schemaVersion: 1
tasks:
  - id: task_daily_water
    taskType: A
    title: 今天有好好喝水吗
    rewardBundleId: rb_task_a_small
    group: DAILY
    maxCompletionsPerDay: 3
    diminishingReturns: true

  - id: task_focus_25
    taskType: B
    title: 专注 25 分钟
    rewardBundleId: rb_task_verify_bonus
    rewardVerifiedBundleId: rb_empty   # 可选：B 类验证即完成，可只用一个包
    handlerParams:
      durationSec: 1500
    group: DAILY
```

---

## 4. 随机事件 `EventDef`

**路径建议**：`config/events/*.yaml`

### 4.1 字段规范

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `id` | 是 | string | |
| `weightBase` | 是 | int | ≥0 |
| `tags` | 否 | list[string] | 用于被动技能改权重 |
| `introTextKey` | 是 | string | 引子文案 |
| `branches` | 是 | list | 至少 1 个选项 |

**`branches[]` 元素**

| 字段 | 必填 | 类型 | 说明 |
|------|------|------|------|
| `optionId` | 是 | string | 稳定 id |
| `labelKey` | 是 | string | 按钮文案 |
| `requiresSkillId` | 否 | string | 填则须拥有该技能且未 CD |
| `energyCost` | 否 | int | 可选扣 `VITALITY`（先 clamp） |
| `rewardBundleId` | 是 | string | |
| `cooldownKey` | 否 | string | 分支级冷却键 |
| `hiddenUnlessMinBond` | 否 | int | 羁绊隐藏选项 |

### 4.2 示例

```yaml
schemaVersion: 1
events:
  - id: evt_sandstorm_lite
    weightBase: 10
    tags: [weather]
    introTextKey: evt.sandstorm.intro
    branches:
      - optionId: use_barrier
        labelKey: evt.sandstorm.use_skill
        requiresSkillId: skill_barrier
        rewardBundleId: rb_evt_skill_use
      - optionId: observe
        labelKey: evt.sandstorm.observe
        rewardBundleId: rb_evt_observe
```

---

## 5. 祈愿 `GachaPoolDef` + 行 `GachaRowDef`

**路径建议**：`config/gacha/pools/*.yaml`

### 5.1 池

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | 如 `pool_normal` |
| `cost` | 是 | `{ currencyId: amount }` 或 `ticketItemId` |
| `pityRule` | 否 | `{ rarePullsMax: 90, rareTier: RARE }` 示例结构，实现解析 |

### 5.2 池内行

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | |
| `poolId` | 是 | 外键 |
| `weight` | 是 | int >0 |
| `rewardBundleId` | 是 | 抽中即派发 |

---

## 6. 道具 `ItemDef` · Buff `BuffDef` · 技能 `SkillDef`

### 6.1 ItemDef — `config/items/*.yaml`

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | |
| `kind` | 是 | `FRUIT` \|`SHARD` \|`COSMETIC` \|`SKILL_CARD` \|`CURRENCY_PACK`（可选细类） |
| `maxStack` | 否 | 默认 999 |
| `useRewardBundleId` | 否 | 果实「使用」时派发的包（可带 buff） |
| `skillCardId` | 否 | kind=SKILL_CARD 时指向 `SkillDef.id` |

### 6.2 BuffDef — `config/buffs/*.yaml`

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | |
| `mergePolicy` | 是 | `REFRESH_DURATION` \|`STACK_INTENSITY_MAX_3` 等枚举 |
| `statMultiplier` | 否 | map `statKey → float` 任务奖励乘区 |
| `statFlatBonus` | 否 | map `statKey → int` 展示加成 |
| `defaultDurationSec` | 否 | use 时未指定则用 |

### 6.3 SkillDef — `config/skills/*.yaml`

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | |
| `kind` | 是 | `passive` \|`active` \|`cosmetic` |
| `cooldownSec` | 否 | active 用 |
| `eventWeightModifiers` | 否 | `{ tag: deltaWeight }` |
| `cosmeticIdleKey` | 否 | cosmetic 用 |

---

## 7. 皮肤 `SkinDef`

**路径建议**：`config/skins/*.yaml`

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | |
| `rarity` | 否 | `N` `R` `SR` `SSR` |
| `shardItemId` | 否 | 碎片 item |
| `shardCost` | 否 | 合成数量 |
| `equipStatDelta` | 否 | map，**可选**装备时常驻小加成（慎用） |
| `bondBonusOnEquip` | 否 | int |

---

## 8. 签到 `SignInRuleDef`（可多张表文件）

**路径建议**：`config/signin/streak.yaml`、`config/signin/monthly.yaml`

### 8.1 连续天规则

| 字段 | 必填 | 说明 |
|------|------|------|
| `streakDay` | 是 | int，第几天 |
| `rewardBundleId` | 是 | |

### 8.2 自然月累计（可选）

| 字段 | 必填 | 说明 |
|------|------|------|
| `daysInMonth` | 是 | 当月签满几天领 |
| `rewardBundleId` | 是 | |

---

## 9. 引用关系图（扩表时禁止环）

```text
TaskDef ──rewardBundleId──► RewardBundleDef
TaskDef ──rewardVerifiedBundleId──► RewardBundleDef
EventDef.branches[] ──rewardBundleId──► RewardBundleDef
GachaRowDef ──rewardBundleId──► RewardBundleDef
SignInRuleDef ──rewardBundleId──► RewardBundleDef
ItemDef ──useRewardBundleId──► RewardBundleDef
RewardBundleDef ──items[]/buffs[]──► ItemDef / BuffDef
ItemDef(SKILL_CARD) ──skillCardId──► SkillDef
EventDef ──requiresSkillId──► SkillDef
```

**禁止**：`RewardBundleDef` 再引用会二次触发派发的未定义行为；物品使用若需链式发奖，应在一个 `RewardBundle` 内列全。

---

## 10. 校验清单（CI 或打包前跑）

1. **唯一性**：所有 `id` 全局唯一。  
2. **引用闭合**：每条外键（`rewardBundleId`、`itemId`、`buffId`、`skillId`、`poolId`）均可解析。  
3. **枚举合法**：`statKey`、`currencyId`、`taskType`、`reasonCode` 均在白名单。  
4. **数值范围**：货币、count ≥0；`weight` >0；`statDelta` 在策划允许范围内（可配每 stat 最大单步增量）。  
5. **任务类型**：`handlerParams` 仅含该 `taskType` 允许键（警告级）。  
6. **事件**：每个 `evt_` 至少一个 branch；`requiresSkillId` 须在 `SkillDef` 存在。  
7. **版本**：`schemaVersion` 与加载器支持版本一致。

---

## 11. 新增内容最小步骤（操作说明）

| 你想加… | 步骤 |
|---------|------|
| 新任务 | ① 在 `rewards` 增 `rb_*`（或复用已有）② 在 `tasks` 增一条 `TaskDef`，填 `rewardBundleId` 与类型参数 |
| 新奖励组合 | 只增 `RewardBundleDef`，别处引用 id |
| 新事件 | ① 增 `rb_*` ② 增 `EventDef` 分支指向包 ③ 若需技能，先 `SkillDef` + 卡池或任务产出 |
| 新卡池掉落 | 增 `GachaRowDef`，`rewardBundleId` 指向含物品/货币的包 |
| 新皮肤 | `SkinDef` + 通常一条 `GachaRow` 或商店行指向含皮肤或碎片的包 |

---

## 12. 修订记录

- **V1.0**：首版规范，约束奖励包、任务、事件、祈愿、道具/Buff/技能、皮肤、签到的扩表格式与校验。
