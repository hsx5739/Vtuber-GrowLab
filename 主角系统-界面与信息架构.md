# 主角系统：界面与信息架构规划（V0.1）

本文与《主角系统-需求整理（V0.2.4）》《属性任务与抽奖细化（V0.2.2）》配套，描述 **App 内如何分区布局**，以承载当前已规划的全部能力。机制层闭环见《主角系统-游戏机制总纲》。实现层为 **KMP**：业务逻辑共享，**UI 可按 Compose Multiplatform 或各端原生**落地，本文不绑定具体控件库。

---

## 1. 设计原则

- **首页即陪伴**：打开 App 第一眼是**陪伴者 + 宿主状态**，符合「情绪陪伴优先」。
- **少层级**：高频动作（聊天、领今日任务、抽一次）**≤ 2 步**触达。
- **系统感但不吓人**：随机事件、技能发动用**弹层/卡片**，禁止仿系统级全屏警报样式。
- **功能有处可找**：抽卡、背包、商店、设置**不藏在二级深坑**，Demo 也要能演示全链路。

---

## 2. 信息架构总览

### 2.1 推荐：底部导航 **5 Tab**（完整版）

| Tab | 名称（示例） | 承载能力 |
|-----|--------------|----------|
| 1 | **主页** | 陪伴者立绘、宿主属性摘要、签到入口、随机事件入口/气泡、快捷进聊天 |
| 2 | **聊天** | LLM 陪聊、对话型任务（D 类）跳转、可选「任务中上下文」 |
| 3 | **任务** | 今日/周常、A～H 全类任务列表与详情（计时 B、健康 C、拍照 F、语音 G） |
| 4 | **祈愿** | 抽卡、星尘/月华展示、概率与规则入口、抽卡动画与结果 |
| 5 | **行囊** | 技能卡、果实、皮肤与碎片、虚拟道具；**商店/充值**入口放本 Tab 顶部或二级页 |

**「我的 / 设置」**：建议作为 **行囊 Tab 右上角齿轮** 进入二级页（账号、隐私、健康权限、关于、内容安全提示），避免第 6 个 Tab。

### 2.2 Demo 裁剪：底部 **4 Tab**（省一条开发量）

| Tab | 合并方式 |
|-----|----------|
| 主页 | 保留；**聊天**改为首页右下角 **悬浮按钮（FAB）** 打开全屏聊天 |
| 任务 / 祈愿 / 行囊 | 与完整版相同 |

---

## 3. 全功能 → 界面映射表

| 能力 | 主要界面 | 辅助入口 / 形态 |
|------|----------|-----------------|
| 陪伴者 + 皮肤 | 主页中央立绘；行囊内换装 | 抽卡获得皮肤后弹窗 → 「去换上」 |
| 宿主属性（气运/能量/心境/羁绊/专注） | 主页顶部或底部 **属性胶囊/横滑卡片**；点进 **属性详情半屏** | 任务完成、签到、事件结束后 **Toast + 条动画** |
| 签到（E 类相关） | 主页 **「今日签到」**按钮；首日可自动弹 **签到日历半屏** | 连续天数在签到页展示 |
| AI 陪聊 | **聊天 Tab** 或 **FAB 全屏** | 主页陪伴者旁 **「和 Ta 说说话」** |
| 任务 A～H | **任务 Tab** 列表 + **任务详情页** | D 类可 deep link 到聊天；H 类由事件弹层完成 |
| 计时任务 B | 任务详情内 **嵌入式计时器** | 完成后回到任务列表刷新 |
| 健康数据 C | 任务详情内说明 + **跳转系统授权**；未授权时显示低配完成说明 | 设置里健康开关 |
| 拍照 F / 语音 G | 任务详情内 **拍照 / 按住说话** | 权限拒绝时显示「仅自述完成」路径 |
| 抽卡 + 星尘/月华 | **祈愿 Tab** | 主页顶部可展示星尘数快捷栏 |
| 技能卡 | **行囊 → 技能** 分栏；事件弹层中展示「可发动」 | 抽卡结果页「放入行囊」 |
| 果实等消耗品 | **行囊 → 道具**；使用后在主页或详情反馈 buff | — |
| 皮肤碎片 | **行囊 → 外观** | 祈愿结果、任务奖励 |
| 随机事件 H | **全局模态卡片**（从主页进入最自然，也可任意页顶飘条） | 主页 **气泡角标**「系统讯息」 |
| 商店/充值 | **行囊内「商店」** 或顶部 Tab 切换 | Demo 可仅占位页 |
| 概率公示 / 合规文案 | 祈愿页 **「规则说明」** 二级页 | — |
| 隐私 / 健康 / 关于 | **设置**（行囊-齿轮） | — |

---

## 4. 各屏布局细化（线框级）

### 4.1 主页（Tab：主页）

**纵向分区（自上而下）**

1. **顶栏**  
   - 左：可选问候语 / 日期  
   - 右：**星尘**（+ 月华若已开）小图标与数字；**设置**可不放此页，统一进行囊  

2. **宿主属性区**（占屏宽约 **全宽**，高度紧凑）  
   - 方案：**一行 4 个胶囊**（气运｜能量｜心境｜羁绊），**专注**可第二行或收入「更多属性」  
   - 点击任一胶囊 → **半屏面板**：当前数值、今日变化原因摘要、一句陪伴者解说（非表格堆砌）

3. **主视觉区（陪伴者）**  
   - 居中偏下 **60% 视觉权重**，保证角色清晰  
   - 轻交互：点击陪伴者 → **随机一句 idle 台词** 或 **小动效**（可选）  
   - **能量低** 时立绘略蔫（滤镜/姿态差分），与需求文档一致  

4. **主页快捷按钮带**（立绘下方一条）  
   - **签到**  
   - **今日任务**（跳转任务 Tab 或任务半屏）  
   - **祈愿一次**（跳转祈愿 Tab 或直接半屏抽卡，Demo 二选一）  
   - 若有未读 **随机事件**：**「系统讯息」** 带红点  

5. **底栏**  
   - 系统 `BottomNavigation` 占满底部  

**随机事件（H）**  
- 触发时：**半透明遮罩 + 居中卡片**（引子文案 → 选项按钮 → 结果 → 关闭）  
- **禁止**全红仿预警样式；标题用「系统沙盘」「剧情事件」等虚构标签  

---

### 4.2 聊天（Tab：聊天 或 FAB 全屏）

**布局**

- **顶栏**：返回（若 FAB 模式则关闭）、清空会话（可选）、陪伴者小头像 + 昵称  
- **中间**：消息列表（用户右、陪伴者左）；支持长按复制等基础能力  
- **底栏**：输入框 + 发送；可选 **语音输入**（与 G 类任务可复用组件）  
- **系统插入条**：当存在 **D 类对话任务** 时，顶栏下 **细条提示**「今日话题：……」点一下展开  

---

### 4.3 任务（Tab：任务）

**布局**

1. **顶栏**：标题「今日委托」；右侧 **周常进度** 入口（小图标）  
2. **分段控件**：`今日` | `本周`（或「周常」单独子页）  
3. **列表单元**（每条任务一行卡片）  
   - 左：类型角标（A～H 可用图标区分，不必露字母）  
   - 中：标题 + 一行说明  
   - 右：状态（未完成 / 验证中 / 已完成）  
4. **点击进入任务详情页**  
   - 通用：**标题、说明、奖励预览**  
   - **A**：主按钮「我完成了」+ 小字「未做仅记录」说明  
   - **B**：大计时器 + 开始/结束  
   - **C**：步数/睡眠展示 + 授权按钮 + 刷新  
   - **D**：按钮「去和 Ta 聊」→ 跳转聊天  
   - **F**：相机/相册 + 识图结果反馈 + 「验证领取加成」  
   - **G**：录音 + 时长提示 + 验证结果  
   - **H**：一般不在此列表常驻，由事件触发；若做「剧情回放」可放列表最下「最近事件」  

---

### 4.4 祈愿（Tab：祈愿）

**布局**

1. **顶栏**：标题「祈愿」；右侧 **记录/规则**（概率说明、历史记录 Demo 可占位）  
2. **货币条**：星尘、月华、单抽券（若有）  
3. **卡池区**：卡池封面 + 名称 + **单抽 / 十连**（Demo 可仅单抽）  
4. **底部**：小字入口「概率公示」「适龄提示」  
5. **抽卡结果**：全屏或半屏 **翻牌动画** → 奖励列表 → **前往行囊** / **再抽**  

---

### 4.5 行囊（Tab：行囊）

**布局**

1. **顶栏**：标题「行囊」；左/右 **商店** 入口、**设置（齿轮）**  
2. **子 Tab 或分段**：`技能` | `道具（果实）` | `外观（皮肤/碎片）`  
3. **列表/宫格**  
   - **技能**：卡面缩略图 + 名称 + 被动说明 / 主动「仅在事件中可用」标签  
   - **道具**：图标 + 数量 + 使用  
   - **外观**：当前穿戴大预览 + 已拥有列表，**一键穿戴**  
4. **商店（二级页）**：商品列表、充值档位（Demo 占位）  

---

### 4.6 设置（二级页）

- 账号与同步（若后期）  
- **隐私政策**、**多模态数据说明**（拍照/语音）  
- **健康数据**开关与说明  
- 通知、音效、陪伴者音量  
- 关于与版本号  

---

## 5. 关键流程的界面串联（文字版）

1. **打开 App** → 主页 → 若有事件：弹 **事件卡片**（H）→ 选分支 → 关闭回主页，属性条播放变化。  
2. **签到** → 主页点签到 → 半屏日历 → 领奖 → 星尘/羁绊反馈。  
3. **做任务** → 任务 Tab → 详情（B/F/G…）→ 完成 → 返回列表；若奖励含抽卡券，**条提示「去祈愿」**。  
4. **抽卡** → 祈愿 Tab → 动画 → 得技能卡/皮肤碎片 → 引导 **行囊** 查看。  
5. **事件发动技能** → 事件卡片中选项「发动 xxx」→ 校验是否拥有技能 + CD → 播放短演出 → 结算奖励。  
6. **聊天** → 聊天 Tab/FAB → 普通对话；D 类任务可通过提示条引导话题。  

---

## 6. ASCII 结构示意（主页）

```
┌─────────────────────────────┐
│ 早安 · 3/27      星尘 120 ✦ │  ← 顶栏
├─────────────────────────────┤
│ [气运][能量][心境][羁绊]      │  ← 属性胶囊，可横向滑动
├─────────────────────────────┤
│                             │
│        ┌───────────┐        │
│        │  陪伴者    │        │  ← 立绘 / Live2D
│        │  立绘区    │        │
│        └───────────┘        │
│                             │
│  [签到] [今日任务] [祈愿]    │  ← 快捷带；事件有红点加 [系统讯息]
├─────────────────────────────┤
│  🏠    💬    📋    ✨    🎒  │  ← BottomNavigation
└─────────────────────────────┘
```

---

## 7. 与 KMP 实现的对应关系（简要）

- **共享**：任务状态机、事件配置 ID、抽卡概率表、属性计算公式、导航路由枚举。  
- **各端差异**：相机、麦克风、健康 Kit、应用内购买，通过 **expect/actual** 或平台 Service 注入；**UI 布局结构保持一致**，遵循本文分区即可。  

---

## 8. 待 UI 设计阶段拍板

- [ ] 视觉风格：偏**治愈浅色**还是**系统科技深色**。  
- [ ] 属性区用「胶囊」还是「环形五维图」（环形图信息密度低，慎选）。  
- [ ] 聊天独立 Tab 与 FAB 方案最终选型。  
- [ ] 随机事件卡片的美术模板（统一边框，避免误解为真警报）。  

---

## 9. 修订记录

- **V0.1**：首版，覆盖当前文档中全部功能对应的界面与导航。
